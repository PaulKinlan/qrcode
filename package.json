{
  "name": "qr-snapper",
  "version": "0.2.5",
  "description": "A simple PWA QR code scanner",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "serve": "vite preview --port 8080",
    "clean": "rm -rf dist .tmp"
  },
  "devDependencies": {
    "vite": "^6.4.1",
    "vite-plugin-pwa": "^0.21.2"
  },
  "dependencies": {
    "comlink": "^4.4.2"
  }
}
